services:
  thinkai-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: thinkai-agent
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - thinkai-voice-agent/.env
    volumes:
      - agent-data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: thinkai-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  agent-data:
