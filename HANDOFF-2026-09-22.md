# HANDOFF — eaisyDesk session-összefoglaló | 2026-09-21/22

**Branch**: `rebuild` (push: `origin/rebuild`) · **Staging**: https://digideskadmin.molaire.hu · **Utolsó commit**: `d4d359f` · **Konténer**: healthy, 0 ERROR · **Lefedett időszak**: 2026-09-13 (este) → 2026-09-22 — összesen **74 commit**.

Ez a fájl a 09-13-i handoff (`HANDOFF-2026-09-13.md`) óta végzett **összes** munkát tematikusan összegzi. A napi munkarend változatlan: minden instrukció után `HANDOFF.md` frissítése + push.

---

## 1. Témakörök és szállítások

### 1.1 Jogosultsági rendszer (admin/member mátrix + manager-kivezetés) — `5a5dcdc`

User-mátrix alapján: **admin = klinikavezető/tulajdonos, member = recepciós**.

- `require_admin_or_manager` guard **törölve** (29 endpoint átsorolva): admin-only lett a user-mgmt, insights, bulk törlések, ügyfél-törlés, szolgáltatások, klinikák, SIP-hívás, kampány start/stop/close/schedule; **membernek megnyílt**: új ügyfél, profil-PUT (az `assigned_to`/`felelos` mező megőrződik membernél — admin-only felelős-hozzárendelés), custom mezők, kanban kártyamozgatás, kézi naptár CRUD, teendők (törléssel), kampány-tervezet CRUD + Gemini.
- Kampány member-szabály: törlés/tartalom csak `Vázlat`/`Tervezet` státuszban.
- **Manager szerepkör kivezetve**: minden manager ADMINNÁ minősült (staging DB: `UPDATE admin_users SET role='admin' WHERE role='manager'` — ⚠️ prodon is kell); AuthContext a régi JWT-ket adminnak normalizálja; role-opciók mindenhol admin|member.
- Member **minden ügyfelet/eseményt/interakciót lát** (assigned-szűrések kivezetve: KanbanPage, InteractionsPage, CalendarPage, ClientsPage).
- Route-gatek: `/settings/*`, `/automatizaciok`, `/analytics` = `AdminOnlyRoute` (URL-védelem is, nem csak menürejtés). „Automatikus értesítések" almenü admin-only (teljes tartalma admin-funkció).
- Verifikáció: vert-token API-smoke (member 8×200 engedélyezett, 9×403 tiltott; admin regresszió).

### 1.2 UX-stabilitás — `d84ed0a`, `b8c8535`

- **Skeleton-glitch fix**: a 30 mp-es polling-hookok minden körben `setLoading(true)`-t hívtak → a teljes oldaltartalom vázlapra villant. Minden hook (useSessions, useGroupedSessions, useCalendarEvents, useKanbanColumns + useClients) csendes háttér-pollt kapott (skeleton csak első betöltésre) + no-op JSON-szűrő (változatlan adat → nincs újrarender).
- **Fejléc-design**: céginfo/szabályok fejlécek sorokra bontva (cím bal, CTA jobb, alatta tartalom — a fix bell-ikon nem takarja); `.page-title` egységesítés; `co-sec-title` hierarchia; duplikált mentés-gomb megszüntetve a szabályokon (ih-save-request esemény).

### 1.3 Ügykezelési/foglalási szabályok redesign — `113fb8b`

- A szabályok fül a céginfo co-* designját kapta + breadcrumbs + „Utolsó módosítás" fejléc (az issue-handling mentés is bumpolja).
- **„Értesítendő" email mezők megszűntetve mindenhol** — a funkció sosem volt bekötve (a küldő kód `surgos/kiemelt/urgent` priority-t keresett, a UI `onallo/jovahagyas/ember`-t mentett).
- Módosítás/lemondás eljárás-dropdownök kivezetve → a két szabad-szöveges mező (`modositas_szoveg`, `figyelmezteto_szoveg`) vezérli a `_format_cancellation_policy`-t (text-vezérelt — a legacy enum-inkompatibilitás is megszűnt vele).

### 1.4 Értékesítési címkék konszolidálása — `983e24b`, `c32e031`

- **Kanonikus kör** (user-döntés): `kampánylead, potenciális ügyfél, árkérdés, törölt időpont, no-show` (+VIP attribútum, nem sales). Backend 3 kiosztási pont + Gemini secondary_tags átírva; staging DB 4 ügyfél `kampány lead` → `kampánylead` átnevezve.
- A voice-prompt címkeblokkja a DB-template-ben a RÉGI kör volt → szinkronizálva (`c32e031`): kanonikus kör + „KIZÁRÓLAG ezekkel" + a sales-címkék kanban-szabálya. ⚠️ **prod `text_configs.system_prompt` címkeblokkja is frissítendő!**
- `potenciális ügyfél` = „érdeklődött, de nem foglalt" → sikeres `book_meeting` után automatikusan törlődik (konverzió).

### 1.5 Ügyfél-integritás: split-brain + duplikátum-kezelő rendszer — `0526efd`, `656755b`, `9326e34`

- **`resolve_client_identity`** (database.py): erős kulcsok (phone > messenger > email) KÜLÖN egyeznek; eltérésnél primary + conflict_id + `duplicate_suspect` jelző mindkét rekordon. Bekötve: voice klasszifikáció, `book_meeting` (+ a hívó VALÓS SIP-száma is erős kulcs — `656755b`).
- **Név-konzisztencia** (`9326e34`): erős-kulcs-egyezés + nulla név-token-átfedés → duplicate_suspect (a „konzisztens-de-hamis párok" esete: 175 „Lachner Ödön" vs „Orosz Erika").
- **Merge-flow**: `POST /admin/api/clients/merge` (admin-only) — mező-szintű keep-választás, interakció-átkötés, napló/címke-únió, source `status='merged'` + `merged_into` (visszavonható); profil sárga banner + mezőválasztós modal; merged rekordok rejtve a listában/kanbanon.
- **Profil session-egyeztetés szigorítva**: név-substring és telefon-a-session_id-ben szabályok KIVEZETVE (keresztbe-húzás); a behúzott session sorai client_id-re újraszűrve.
- **`tag_client` az arbiteren** + létrehozás csak erős kulccsal (email/telefon) — puszta névvel soha (adatszegény torzók ellen).

### 1.6 Voice agent — átirat, foglalási lánc, biztonság

**Átirat-megbízhatóság** (`72fceea`, `fd5874e`):
- Ügyféloldali capture: a Gemini preview `input_audio_transcription` hívásonként inkonzisztens → a klasszifikáció **4 forrásból** gyűjt (AgentSession history, realtime `_chat_ctx`, llm chat_ctx, event-list) és az ügyfél-turnust tartalmazót választja; dedup role-onkénti (rövid válaszok nem nyelődnek el).
- **Timezone**: minden transcript-időbélyeg Budapesti (volt: UTC → popup-szakadás + 2 órás eltolódás); a popup append-sorrendet mutat (időbélyeg-sort kivezetve — kevert forrású ts okozta a sorrend-zűrt); client 265 naplója kézzel normalizálva (52 ts).

**Foglalási lánc** (f61ec65/b74dbda/3d1fbf7, 6bc1256, 584e980, fd5874e):
- **Ellátó KÖTELEZŐ minden foglaláshoz** (`resolve_assigned_staff`: explicit → szolgáltatás-névsor → pool; „minden fogorvos" poolból dentálhigiénikusok kizárva); bekötve voice + email pending + messenger/web flow.
- **Időtartam**: szolgáltatás-egyezésnél a TÁBLA nyer az LLM 30 perces defaultjával szemben (`resolve_service_duration`); egyeztetési rangsor mindkét feloldónál: PONTOS > nm⊂cím (leghosszabb) > cím⊂nm.
- **Telefonszám az eseményen**: a SIP-hívószám az elsődleges (ellenőrzött); eltérő bemondott → `custom_data.contact_phone`.
- **Egységes eseménycím**: `<szolgáltatás> - <név>` minden úton (`db.normalize_event_title`) + 6 sor backfill; a profil-kártya/popup a nevet mindkét formátumról levágja (`2273b9f`).
- **Visszaigazoló email**: Brevo 400 = sender_email is elveszett a business_info wipe-nél → prod-értékek visszaállítva + a 115-ös visszaigazoló kézzel újraküldve.
- Módosításnál `reminder_sent` reset (az új időpontra újra kimegy az emlékeztető).

**Voice biztonság A–C kör** (`5aba639`, 11/11 tool-teszt):
- `_find_event_secure`: event_id tulajdon-ellenőrzéssel; cím-keresésnél az owner_email KÖTELEZŐ (a „bárki jövőbeli/legacy" fallback TÖRÖLVE — a 265-ös incidens-osztály); `not_found`/`multiple_matches` (emberi jelöltek) / `need_email`; `original_start_dt` másodfaktor.
- modify/delete naplóz (Módosított/Törölt időpont + diary, csak sikeres művelet után); lemondásnál `törölt időpont` címke + Utánkövetés (session-scope: session-beli újrafoglalásnál lekerül).
- Közös `_validate_slot` (NYITVATARTÁS — eddig csak prompt-szabály volt! — + ütközés + alternatíva), book+modify; modify saját eseményt kizárja.
- `send_followup_email` KI az ALL_TOOLS-ból.
- Prompt 9. szabály: modify/delete email-azonosítás + diszkrimináció.

**Voice viselkedés** (`ed975a3`): hívószám-injekció 5 mp-es retry-ciklussal (a SIP participant késve érkezik); `preemptive_generation=False` (a modell fragmentumokra válaszolt — közbevágás, sorrend-zűr, ismétlések); endpointing 0,3→0,8s / silence 0,3→0,6s.

**find_client tool** (`a785abe`, `46b8f11`, 5/5 teszt): paraméter nélkül a HÍVÓ számára keres (caller-fallback csak teljesen paraméter nélküli hívásnál); telefon-egyezés utolsó 9 számjeggyel (formátum-mentes); sorrend telefon→email→név; egy találat = teljes profil + közelgő időpontok; több = MASZKOLT diszkriminációs lista. Prompt 0. szabály: hívás elején find_client, visszatérő ügyfélnél üdvözlés néven + tárolt adatok NEM újrabekérve (csak megerősítve), érzékeny adat csak második azonosító után.

**Szolgáltatás-szintű foglalási szabályok** (`d339c9d`): `services.note` UI-szerkeszthető („Megjegyzés (foglalási szabály)" oszlop); a higiénia-mentesség az exceptions-ből a Fogkő-eltávolítás note-jába költözött (a Kivételek lista ismét tiszta eszkalációs lista); a kódba égetett mentesség kivezetve, generikus „note > általános szabály" pointer helyette; render-címke `[Foglalási szabály: …]`.

**Alapelvek-csere + kódoldali CSENDES azonosítás** (`2f83b80`, render-ellenőrzés zöld):
- A find_client kivéve a modell kezéből a hívás elején (a GDPR-hozzájárulás előtt hívta — a 0. szabály konfliktusa okozta a minőségromlást: hozzájárulás-válasz kihagyása, korai email-megerősítés). Most a **szerver** oldja fel `resolve_client_identity`-vel és **csendes profil-hintet** injektál: „NE szólítsd név szerint, NE olvasd be az adatokat automatikusan — csak szükség esetén használd (foglalási email-megerősítés: 'a rendszerünkben rögzített <email>-re küldhetjük?', modify/delete tulajdon-ellenőrzés); érzékeny adat csak második azonosító után." (User-finanhangolás: csendes azonosítás — nem név szerinti üdvözlés, nem adat-beolvasás.)
- **ALAPELVEK** (user 6 pontja + `report_alert` névvel) a DB-template tetején + seed: forrás-prioritás (kód/eszköz > szabályok > strukturált adat > céginfo > kampányszöveg > általános tudás), ne-találj-ki, ismert-adat-használat (merev sorrend helyett hiányzó-info-only), telefonos kommunikáció, kitérők/önkorrekció, műveletek csak eszköz-sikerre.
- **`patient_rules` lecsupaszítva**: 0. (find_client-indítás), 5. (merev a–f lépéssorrend), 6., 8. (book_meeting-kötelezettség — Alapelvek §6 + kód fedi) TÖRÖLVE; marad: 1–4 (provider-adat), 7. (PSTN), 9. (modify/delete email-azonosítás). A „túl sok egyenrangú SZIGORÚ szabály" dilúció megszüntetve.

### 1.6b Naptár-üzem javítások — `3ac19f1`, `bfff340`, `9dddd71`+`b1ec547`

- **„Befoglalt időpont" a popupban** (`3ac19f1`): a matchedEvent a LEGKÉSŐBBI eseményt mutatta (start_dt DESC) → több jövőbeli foglalásnál a korábbi ügy dátuma jelent meg. Most az esemény `created_at`-je a legközelebbi az interakció időpontjához.
- **Kézi create/delete 500** (`bfff340`): a szinkron endpointokban `asyncio.create_task` → „no running event loop" 500 a sikeres DB-művelet UTÁN (tünet: „Hiba törléskor" toast + nem frissülő lista, pedig a törlés megtörtént). Mindkét endpoint `async def`. Tanulság: sync FastAPI-endpointban nincs event loop.
- **Kézi törlés 'törölt időpont' rituáléval** (`9dddd71`+`b1ec547`): a kézi naptár-törlés a kapcsolódó ügyfélhez (email→telefon→név) a voice/cancel-link flow-val azonos rituálét alkalmazza: 'törölt időpont' címke (sales-tag → első oszlop) + Utánkövetés státusz + interakció (eredmény: Törölt időpont) + ügyfél-napló. Hibás felvitelnél a címke manuálisan eltávolítható; azonosíthatatlan ügyfélnél nincs címkézés. (FK-fix: a napló-sessiont `create_session`-nel kell létrehozni.)

### 1.7 Email/threading — `8b23961`, `f342bd3`

- **Tárgy-szintű email-threadek**: session_id = `email_<feladó>_<normalizált tárgy>` (Re:/Fwd:/Vá: prefix le, lowercase, 80 kar) — egy ügy = egy thread, külön témák külön napló-sor; a Gemini-kontextus is tárgy-thread szerinti; cancel-link interakció a „Időpont visszaigazolás" threadbe.
- **Csatorna-szűrt Előzmények** a popupban (email-popup csak emailek, hívás-popup csak hívások; a teljes kép az ügyfélprofilon).
- **KRITIKUS hotfix** (`f342bd3`): a tool-log szűrő túl tág volt (`tool_name IS NULL`) → az `imap_worker_ai`/`process_meta_message` stamppelt EMAILEK/messenger eltűntek a naplóból és a profilról. Kizárási lista pontosítva: csak `book_meeting`, `lookup_info`, `check_calendar`. **Tanulság: tool_name ≠ tool-log.**
- `get_grouped_interactions` SQL-függvény frissítve (count + reprezentatív sor tool-log-mentes, a fenti pontosított listával) — `migrate_interaction_count_toollog.sql` a repóban.

### 1.8 Incidensek és tanulságok

| Incidens | Lecke |
|---|---|
| **business_info törlődött** (árlista/GYIK/kampányok/exception-ök): a 09-13-i jogosultsági smoke-teszt részleges POST-tal nullázta ki (pydantic-defaultok) | `save_business_info` most `exclude_unset=True` (`6516bb7`); **smoke-tesztnél soha ne POST-oljunk részleges body-t élő adatra**. Helyreállítás a prod Rivergate-sorból |
| **campaigns.id identity elveszett** (staging) → kampány-létrehozás 500 | Management API-val visszaállítva (RESTART WITH 89) |
| **processed_emails törlése + restart** → a régi teszt-levelek újrafeldolgozódtak | tiszta-lap törlésnél a `processed_emails`-t HAGYNI KELL |
| **detectEredmeny wrapper hiányzott** (commit-üzenet állította, diff nem tartalmazta) | „kész" funkciót a DIFF alapján kell lezárni (`4503d48`) |
| **Triage értesítési emailek sosem működtek** (priority-értékkészlet inkonzisztens) | a mezők megszüntetve (a funkció dead volt) |
| HANDOFF-blokk rossz fájlba íródott (cwd-eltérés) | `af80c57` — commit előtt `git status` |

### 1.9 Prod-deploy (levezényelve) — `fdbd6f1`, `ee8ea20`

- Prod deploy a `592f99e`-ig futó commitokkal, migrációkkal lefutott.
- **Prod hálózati incidens**: a dockerd ipv6-flip miatti DNS/probléma gyökér + javítás (részletek az `ee8ea20` commit-HANDOFF-ban).

---

## 2. Adatbázis/Supabase változások (stagingen ÉLŐK)

```sql
-- LE FUTOTT (Management API-val):
ALTER TABLE public.campaigns ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (RESTART WITH 89);
CREATE OR REPLACE FUNCTION public.get_grouped_interactions(...) -- tool-log-szűrővel (book_meeting/lookup_info/check_calendar)
UPDATE admin_users SET role='admin' WHERE role='manager';
-- DB-szöveg: text_configs.system_prompt címkeblokk = kanonikus kör
-- Adat: business_info helyreállítás a prod Rivergate-sorból (árlista, GYIK, kampány, exceptions, sender_*);
--        exceptions tisztítva (higiénia-szabály → services.note); Fogkő-eltávolítás note beállítva;
--        kampánylead normalizálás (4 ügyfél); 6 eseménycím normalizálva; teszt-ügyfelek/-adatok törölve.
```

### ⚠️ PROD-DEPLOYNÁL KÖTELEZŐEN KIFUTTATANDÓ
1. `UPDATE admin_users SET role='admin' WHERE role='manager';`
2. `migrate_interaction_count_toollog.sql` (a `get_grouped_interactions` új, pontosított definíciója)
3. A staging migrációs lista a 09-13-i handoffból (`calendar_events.status/pending_until`, `interactions.received_at/sent_at/diary_fragment`, `business_info.updated_by`) — ellenőrizni, hogy mind lefutott-e a prod-deploykor.
4. Prod `text_configs.system_prompt` címkeblokk frissítése a kanonikus körre.

---

## 3. Nyitott tételek

| # | Tétel | Állapot |
|---|---|---|
| 1 | **Slot-UI**: ellátónkénti rendelési idő, per-doctor ütközés-ellenőrzés (a „minden orvos mindig rendel" korlát feloldása) | user-restancia — későbbi epikusz |
| 2 | ~~Alapelvek-csere~~ | ✅ KÉSZ (`2f83b80`) — alapelvek a prompt tetején, patient_rules lecsupaszítva, csendes azonosítás a szerveren |
| 3 | **Prod `text_configs.system_prompt`** szinkron (címkeblokk + az **Alapelvek-blokk** is!) | prod-deploy teendő |
| 4 | **267/268** („Pónus"/"Bónus Róbert") merge a merge-felülettel | user döntése |
| 5 | GYIK 3. (üres) tétel törlése/rendezése; lejárt „Szezonális akció" kampány inaktiválása a céginfóban | user UI-feladat |
| 6 | Élő teszthívás-ellenőrzés: GDPR-válasz megvárása + csendes azonosítás (név nélküli beszélgetés, email csak foglalási megerősítéskor) | teszt |
| 7 | Telefonszám-normalizálás kiterjesztése az arbiter egészére (find_client-ben már megvan) | javasolt köv. kör |
| 8 | Prod-deploy a 09-21/22-es commitokkal (voice-biztonság, find_client, threading, service-notes, Alapelvek, naptár-fixek) | külön levezényendő |

---

*Frissítve: 2026-09-22 — a 09-13 óta eltelt összes munka (74 commit) a fenti szerkezetben. A napi részletek a `HANDOFF.md` időrendi bejegyzéseiben.*
